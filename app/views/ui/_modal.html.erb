<%# Use com bloco: o conteúdo do bloco vira o corpo do modal.
    locals: title, trigger_label, trigger_variant (padrão :primary),
            trigger_class (classes extras) ou trigger_classes (substitui todas,
            para gatilhos que não são botão comum — o flutuante, por exemplo),
            open (abre já conectado, para reexibir com erros) %>
<% dialog_id = "modal-#{title.parameterize}" %>
<% trigger_classes = local_assigns[:trigger_classes] ||
     ui_button_classes(variant: local_assigns.fetch(:trigger_variant, :primary),
                       extra: local_assigns[:trigger_class]) %>

<div data-controller="modal" data-modal-open-value="<%= local_assigns.fetch(:open, false) %>">
  <button type="button" data-action="modal#open" class="<%= trigger_classes %>"
          aria-haspopup="dialog">
    <%= trigger_label %>
  </button>

  <dialog data-modal-target="dialog"
          data-action="click->modal#closeOnBackdrop"
          id="<%= dialog_id %>"
          aria-labelledby="<%= dialog_id %>-title"
          <%# m-auto centraliza: o <dialog> modal nasce com inset 0 e conta com o
              margin auto do user-agent para se centrar, e o preflight do
              Tailwind zera margem em tudo — sem isto ele encosta no canto. %>
          class="m-auto w-[min(32rem,calc(100vw-2rem))] rounded-card border border-nude-deep
                 bg-white p-0 text-wine-ink backdrop:bg-wine-ink/50">
    <header class="flex items-center justify-between gap-4 border-b border-nude-deep px-5 py-4">
      <h2 id="<%= dialog_id %>-title" class="text-lg"><%= title %></h2>

      <button type="button" data-action="modal#close" aria-label="Fechar"
              class="inline-flex h-11 w-11 items-center justify-center rounded-control text-clay-text hover:bg-nude">
        <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="1.5" aria-hidden="true">
          <path d="M6 6l12 12M18 6L6 18" stroke-linecap="round" />
        </svg>
      </button>
    </header>

    <div class="px-5 py-5">
      <%= yield %>
    </div>
  </dialog>
</div>

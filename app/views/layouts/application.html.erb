<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <%# Mesmo head das outras telas: é ele que carrega Playfair Display e Inter.
        Nenhum controller cai aqui hoje, mas o dia em que um cair não pode ser o
        dia em que a tipografia do SPEC 05 some. %>
    <%= render "layouts/head", title: "Aurora" %>
  </head>

  <body class="flex min-h-screen flex-col bg-cream">
    <main class="mx-auto w-full max-w-6xl flex-1 px-4 py-8 sm:px-6">
      <%= yield %>
    </main>
  </body>
</html>

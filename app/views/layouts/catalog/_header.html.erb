<%# Faixa vinho com a marca, busca e carrinho. Enxuto. %>
<header class="bg-wine text-nude">
  <div class="mx-auto flex max-w-6xl flex-wrap items-center gap-x-4 gap-y-3 px-4 py-4 sm:px-6">
    <%= link_to "Aurora", root_path, class: "font-display text-xl text-nude" %>

    <%# A busca fica fora do frame, então o campo não é redesenhado a cada
        resposta e o cursor permanece onde está. A página diz em qual frame
        entregar o resultado; sem isso, navega a página inteira. %>
    <% search_frame = content_for(:catalog_search_frame).presence || "_top" %>

    <%= form_with url: root_path, method: :get, role: "search",
          class: "order-last w-full sm:order-none sm:ml-auto sm:w-64",
          data: { turbo_frame: search_frame, controller: "auto-submit",
                  auto_submit_delay_value: 300,
                  action: "input->auto-submit#submit" } do %>
      <label for="catalog-search" class="sr-only">Buscar produtos</label>
      <%= search_field_tag :q, params[:q], id: "catalog-search",
            placeholder: "Buscar produtos...", autocomplete: "off",
            class: "min-h-11 w-full rounded-control border border-wine-dark bg-wine-dark px-3 " \
                   "text-sm text-nude placeholder:text-nude-deep" %>

      <% if params[:category_id].present? %>
        <%= hidden_field_tag :category_id, params[:category_id] %>
      <% end %>
    <% end %>

    <%= render "layouts/catalog/cart" %>
  </div>
</header>

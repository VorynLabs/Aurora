<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <%= render "layouts/head", title: "Aurora" %>
  </head>

  <body class="flex min-h-screen flex-col bg-cream">
    <%= render "layouts/catalog/header" %>

    <main class="mx-auto w-full max-w-6xl flex-1 px-4 py-8 sm:px-6">
      <%= render "ui/flash" %>
      <%= yield %>
    </main>

    <%= render "layouts/catalog/footer" %>
  </body>
</html>
